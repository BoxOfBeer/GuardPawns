Ядро симуляции: планетарные формулы, генерация heightmap, геномы/ДНК, Pawn/PawnAgent и менеджеры агентов; SurfaceSampler — единый источник высоты и уклона поверхности.
Текущее правило: математика CPU-сэмплинга рельефа должна побитно повторять путь GPU (UV с Y-flip, R8 unpack, bilinear, blend, displacement).
