namespace SpaceDNA
{
    /// <summary>
    /// Версия сборки. Поднимается на 0.01 с каждой удачной итерацией (запись в dev.log).
    /// </summary>
    public static class GameVersion
    {
        public const string Current = "0.07";
    }
}
